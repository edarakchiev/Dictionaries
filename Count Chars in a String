text = input()

count_dict = {}

for el in text:
    if el not in count_dict:
        if el == " ":
            continue
        count_dict[el] = 0
    count_dict[el] += 1

for key, value in count_dict.items():
    print(f"{key} -> {value}")
